# Docker Development Environment Variables
# These variables are used by docker-compose.dev.yml

# Database credentials (PostgreSQL + Apache AGE)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quranref
DB_USERNAME=kashif
DB_PASSWORD=compulife

# Application settings
ENVIRONMENT=development
DEBUG=true

JWT_SECRET_KEY=dev-secret-key-must-be-at-least-32-bytes!
FRONTEND_URL=http://localhost:41149
# Note: GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET are in backend/.env (gitignored)
