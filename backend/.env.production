# Production Environment Variables for Backend
# Update these values with your production credentials

# ArangoDB Root Configuration
ARANGO_ROOT_PASSWORD=Test123!

# Environment Settings
ENVIRONMENT=production
DEBUG=false

# Primary Database Configuration (used by the app)
DB_HOSTS=http://arangodb:8529
DB_HOSTS_NORMAL=http://arangodb:8529
DB_HOSTS_DOCKER=http://arangodb:8529
DB_NAME=quranref
DB_USERNAME=quranref
DB_PASSWORD=compulife

# Legacy GDB Configuration (for compatibility)
GDB_SERVER=arangodb
GDB_PORT=8529
GDB_USERNAME=quranref
GDB_PASSWORD=compulife
GDB_DATABASE=quranref

# Static Files Configuration
STATIC_URL=/static/
# STATIC_PATH=static

# Frontend URLs (for CORS and API integration)
DEV_JS_SERVER=https://quranref.info
WEBSITE_BASE_URL=https://quranref.info
VITE_API_BASE_URL=https://api.quranref.info/api/v1
VITE_WEBSITE_BASE_URL=https://quranref.info

# API Configuration
API_V1_STR=/api/v1
CORS_ORIGINS=["https://quranref.info", "https://www.quranref.info", "https://api.quranref.info"]

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOGGING_ENABLED_FOR_PACKAGES=quranref:INFO,arango-orm:WARNING

# Testing Database Configuration (optional)
testing_DB_HOSTS=http://arangodb:8529
testing_DB_NAME=quranref_test
testing_DB_USERNAME=quranref
testing_DB_PASSWORD=compulife

# Security (generate a new secret key for production)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=qref-z5vhLeiHyqS6kT
